diff --git a/accuracy/eval/LongGenBench/eval.py b/accuracy/eval/LongGenBench/eval.py
index 4cf283a..56950da 100644
--- a/accuracy/eval/LongGenBench/eval.py
+++ b/accuracy/eval/LongGenBench/eval.py
@@ -1,3 +1,4 @@
+import os
 import json, os
 import re
 import time
@@ -20,7 +21,8 @@ def parse_args():
     parser = argparse.ArgumentParser(description='Run LLM with command line arguments.')
     parser.add_argument('--data', type=str, required=True, help='data_path')
     parser.add_argument('--csv', type=str, required=True, help='csv_path')
-    parser.add_argument('--gpu', type=int, default=1, help='Number of GPUs to use.')
+    parser.add_argument('--gpu', type=int, default=int(os.environ.get('LGB_JUDGE_TP', 1)),
+                        help='tensor_parallel_size for the judge (Qwen3-32B bf16 needs >=2 on 48GB cards)')
     return parser.parse_args()
 
 
@@ -59,7 +61,10 @@ def create_prompts(checks, type_to_block):
     
     return prompts, identifiers
 
-eval_model_path = "/state/partition/gdliu/huggingface/hub/models--Qwen--Qwen3-32B"
+# Their hardcoded absolute path points at the authors' own filesystem, so the
+# judge cannot be resolved anywhere else. Keep Qwen3-32B as the DEFAULT (that is
+# what the FreeKV numbers in Table 2 were judged with) but allow an override.
+eval_model_path = os.environ.get("LGB_JUDGE_MODEL", "Qwen/Qwen3-32B")
 
 # 定义评估准确性的函数
 def evaluate_accuracy(prompts, llm, sampling_params):
@@ -120,8 +125,15 @@ args = parse_args()
 # Initialize the LLM with the specified model and configuration
 # llm = LLM(model="meta-llama/Llama-3.3-70B-Instruct", tensor_parallel_size=args.gpu)
 # llm = LLM(model="Qwen/Qwen2.5-7B-Instruct", tensor_parallel_size=args.gpu)
+# Qwen3-32B is ~64GB across two 48GB cards; vllm's default
+# gpu_memory_utilization=0.9 plus a 256-request sampler warmup OOMs here
+# ("CUDA out of memory occurred when warming up sampler with 256 dummy
+# requests"). Both are overridable so the judge can run on this hardware.
 llm = LLM(model=eval_model_path, tensor_parallel_size=args.gpu,
-          disable_custom_all_reduce=True)
+          disable_custom_all_reduce=True,
+          gpu_memory_utilization=float(os.environ.get("LGB_JUDGE_GPU_UTIL", 0.90)),
+          max_num_seqs=int(os.environ.get("LGB_JUDGE_MAX_SEQS", 256)),
+          max_model_len=int(os.environ.get("LGB_JUDGE_MAXLEN", 4096)))
 # Define the sampling parameters
 sampling_params = SamplingParams(temperature=0.95, top_p=0.95, max_tokens=50, seed=42)
 
diff --git a/accuracy/eval/LongGenBench/pred.py b/accuracy/eval/LongGenBench/pred.py
index 20d90a3..9ef900e 100644
--- a/accuracy/eval/LongGenBench/pred.py
+++ b/accuracy/eval/LongGenBench/pred.py
@@ -266,14 +266,18 @@ if __name__ == "__main__":
     input_file = f"eval/LongGenBench/Dataset_{args.input_type}.json"
     inputs = load_inputs(input_file)
 
-    prompts = [input_data['prompt'] for input_data in inputs]
+    # BUG (theirs): only `prompts` was sliced, while get_pred does
+    # `input_data = inputs[idx]` with idx enumerating the SLICED prompts. With
+    # --data_from 300 that pairs Block generations with Week metadata (number,
+    # type, checks), so every scored field is for the wrong item. Slice both.
     if args.data_idx is not None:
-        prompts = [prompts[args.data_idx]]
+        inputs = [inputs[args.data_idx]]
     elif args.data_from is not None:
         assert args.data_idx_to is not None
-        prompts = prompts[args.data_from: args.data_idx_to]
+        inputs = inputs[args.data_from: args.data_idx_to]
     elif args.data_idx_to is not None:
-        prompts = prompts[:args.data_idx_to]
+        inputs = inputs[:args.data_idx_to]
+    prompts = [input_data['prompt'] for input_data in inputs]
 
     model_name = args.model
     model, tokenizer, step_updater, eos_token_ids, config = load_model_and_tokenizer(
diff --git a/accuracy/eval/reasoning/pred.py b/accuracy/eval/reasoning/pred.py
index 47f4b0f..120102a 100644
--- a/accuracy/eval/reasoning/pred.py
+++ b/accuracy/eval/reasoning/pred.py
@@ -320,6 +320,41 @@ if __name__ == "__main__":
         data = data.select(range(args.data_from, len(data)))
     
     out_path = get_out_path(args, config)
+    # Resume: results are APPENDED, so re-running a cell would duplicate every
+    # record rather than skip it.  Drop the problems already written for this
+    # exact (model, dataset, method, eta, seed) cell.  Cells here run for hours,
+    # so a restart must not have to redo completed work -- or silently
+    # double-count it into the pass@k / avg@k denominators.
+    if os.path.exists(out_path):
+        _done = set()
+        with open(out_path, encoding="utf-8") as _f:
+            for _line in _f:
+                _line = _line.strip()
+                if _line:
+                    try:
+                        _done.add(json.loads(_line)["qid"])
+                    except Exception:
+                        pass
+        if _done:
+            # qid is `enumerate index + data_from`, so the remaining problems
+            # must keep their ORIGINAL numbering -- selecting a subset would
+            # renumber them from 0 and collide with the qids already written.
+            # Records are appended in order, so what is done is a prefix:
+            # advance data_from past it instead of reindexing.
+            _base = args.data_from or 0
+            _next = max(_done) + 1
+            _skip = _next - _base
+            print(f"[resume] {len(_done)} already done in {os.path.basename(out_path)}; "
+                  f"resuming at qid {_next}")
+            if _skip >= len(data):
+                print("[resume] cell already complete, nothing to do")
+                raise SystemExit(0)
+            if _skip > 0:
+                if len(_done) != _next - _base:
+                    print(f"[resume] WARNING: {_next - _base - len(_done)} gap(s) in "
+                          f"qids below {_next} will NOT be re-run")
+                data = data.select(range(_skip, len(data)))
+                args.data_from = _next
     if args.batch_size == 1:
         preds = get_pred(
             model,
diff --git a/accuracy/eval/util.py b/accuracy/eval/util.py
index e4bd935..a872503 100644
--- a/accuracy/eval/util.py
+++ b/accuracy/eval/util.py
@@ -1,3 +1,4 @@
+import os
 import os, argparse
 import numpy as np
 from pathlib import Path
@@ -31,7 +32,11 @@ def parse_common_args(parser):
                         choices=["full", "duo_attn", "razor", 
                                  "quest", "arkv", "spec_ret", "raas"],
                         help="KV cache eviction/selection method")
-    parser.add_argument("--page_rep", type=str, default="quest", choices=["quest", "arkv"],
+    parser.add_argument("--eta", type=float, default=0.0,
+                        help="OVAL output-aware mix; 0 = key PCA (the LOCKS special case)")
+    parser.add_argument("--oval_rank", type=int, default=8,
+                        help="OVAL basis rank")
+    parser.add_argument("--page_rep", type=str, default="quest", choices=["quest", "arkv", "oval"],
                         help="Page representation method")
     parser.add_argument("--GQA_policy", type=str, default="avgS",
                         choices=["maxQ", "avgQ", "maxS", "avgS", "maxSM", "avgSM"],
@@ -128,6 +133,15 @@ def get_out_path(args, config, out_root_dir=None, mkdir=True):
             else:
                 GQA_policy = config["GQA_policy"]
                 out_path += f"-{GQA_policy}"
+                # Record the page representation in the FILENAME. Without this
+                # an OVAL run and a quest run with otherwise identical settings
+                # write to the SAME path and silently overwrite each other.
+                if config.get("page_rep", "quest") != "quest":
+                    out_path += f"-{config['page_rep']}"
+                    if config.get("page_rep") == "oval":
+                        out_path += (f"_r{config.get('oval_rank', 8)}"
+                                     f"_eta{config.get('eta', 0.0)}"
+                                     f"_q{int(os.environ.get('OVAL_QUANT', '1') != '0')}")
                 if method == "spec_ret":
                     spec_ret_steps = config["spec_ret_steps"]
                     llb = config["llb"]
@@ -213,6 +227,14 @@ def load_model_and_tokenizer(path, args):
         low_cpu_mem_usage=True,
         attn_implementation="eager",
         device_map = "auto",
+        # Without this, device_map="auto" keeps a conservative headroom and, for
+        # Qwen-14B at 64K, decides the model does not fit and offloads layers to
+        # META tensors -- which then dies inside the patch with
+        # "aten::nonzero ... with Meta tensors". Raising the per-GPU cap keeps
+        # every layer on device. KVC_MAX_MEMORY="45GiB" (per visible GPU).
+        **({"max_memory": {i: os.environ["KVC_MAX_MEMORY"]
+                           for i in range(torch.cuda.device_count())}}
+           if os.environ.get("KVC_MAX_MEMORY") else {}),
     )
 
     generation_config = GenerationConfig.from_pretrained(path)
@@ -271,6 +293,8 @@ def load_model_and_tokenizer(path, args):
         config["recent"] = recent_size
         config["skip_layer"] = args.skip_layer
         config["page_rep"] = args.page_rep
+        config["eta"] = args.eta
+        config["oval_rank"] = args.oval_rank
         print(f"Enabling {method} evaluation using sink size {sink_size} recent size {recent_size}, "
               f"skip {args.skip_layer} layers")
         if method in ["quest", "arkv", "spec_ret"]:
diff --git a/accuracy/kvc/patch/dynamic_attention.py b/accuracy/kvc/patch/dynamic_attention.py
index 32ac2af..764cb2f 100644
--- a/accuracy/kvc/patch/dynamic_attention.py
+++ b/accuracy/kvc/patch/dynamic_attention.py
@@ -14,6 +14,9 @@ def repeat_kv_BLH(hidden_states: torch.Tensor, n_rep: int) -> torch.Tensor:
     return hidden_states.reshape(batch, slen, num_key_value_heads * n_rep, head_dim)
 
 
+from .oval import build_summary as _oval_build, oval_sel
+
+
 def quest_sel(q, min_k_for_sel, max_k_for_sel, GQA_policy, num_heads, num_kv_heads):
     if GQA_policy in ["maxQ", "avgQ"]:
         # [bsz, 1, num_kv_heads, head_dim]
@@ -103,7 +106,18 @@ def quest_arkv_attn(self,
             )
             mins = paged_k.min(dim=2).values
             maxs = paged_k.max(dim=2).values
-            if self.page_rep == "arkv":
+            if self.page_rep == "oval":
+                # our summary needs VALUES too when eta > 0; their digest path
+                # only ever looks at keys
+                paged_v = v[:, sink_size: sink_size + num_init_pages*page_size, :, :].reshape(
+                    v.shape[0], num_init_pages, page_size, num_kv_heads, head_dim
+                )
+                _mu, _b, _c = _oval_build(paged_k, paged_v, self.oval_Gh,
+                                         self.oval_eta, self.oval_rank)
+                self.oval_mu[:, :num_init_pages, ...] = _mu
+                self.oval_basis[:, :num_init_pages, ..., :_b.shape[-1]] = _b
+                self.oval_coef[:, :num_init_pages, ..., :_c.shape[-1]] = _c
+            elif self.page_rep == "arkv":
                 centers = (maxs + mins) / 2
                 dists = (
                     (
@@ -124,7 +138,16 @@ def quest_arkv_attn(self,
         )
         mins = new_paged_k.amin(dim=1)
         maxs = new_paged_k.amax(dim=1)
-        if self.page_rep == "arkv":
+        if self.page_rep == "oval":
+            new_paged_v = v[:, sink_size + self.num_pages*page_size:, :, :].reshape(
+                v.shape[0], page_size, num_kv_heads, head_dim
+            )
+            _mu, _b, _c = _oval_build(new_paged_k.unsqueeze(1), new_paged_v.unsqueeze(1),
+                                     self.oval_Gh, self.oval_eta, self.oval_rank)
+            self.oval_mu[:, self.num_pages, ...] = _mu[:, 0]
+            self.oval_basis[:, self.num_pages, ..., :_b.shape[-1]] = _b[:, 0]
+            self.oval_coef[:, self.num_pages, ..., :_c.shape[-1]] = _c[:, 0]
+        elif self.page_rep == "arkv":
             centers = (maxs + mins) / 2
             dists = (
                 (
@@ -146,6 +169,12 @@ def quest_arkv_attn(self,
         new_max_k = torch.empty((1, self.alloc_len, self.max_k.shape[-2], self.max_k.shape[-1]), 
                                 device=self.max_k.device, dtype=self.max_k.dtype)
         self.max_k = torch.cat([self.max_k, new_max_k], dim=1)
+        if self.page_rep == "oval":
+            for _n in ("oval_basis", "oval_coef", "oval_mu"):
+                _t = getattr(self, _n)
+                _new = torch.empty((_t.shape[0], self.alloc_len, *_t.shape[2:]),
+                                   device=_t.device, dtype=_t.dtype)
+                setattr(self, _n, torch.cat([_t, _new], dim=1))
     
     if update_only:
         return None
@@ -171,6 +200,18 @@ def quest_arkv_attn(self,
     # [1, num_used_pages, num_kv_heads, head_dim]
     min_k_for_sel = self.min_k[:, :self.num_pages - num_recent_page, ...]
     max_k_for_sel = self.max_k[:, :self.num_pages - num_recent_page, ...]
+    _use_oval = self.page_rep == "oval"
+    if _use_oval:
+        _n_sel = self.num_pages - num_recent_page
+        _ob = self.oval_basis[:, :_n_sel, ...]
+        _oc = self.oval_coef[:, :_n_sel, ...]
+        _om = self.oval_mu[:, :_n_sel, ...]
+
+    def _score(qq):
+        if _use_oval:
+            return oval_sel(qq, _ob, _oc, _om, self.GQA_policy, num_heads, num_kv_heads)
+        return quest_sel(qq, min_k_for_sel, max_k_for_sel, self.GQA_policy,
+                         num_heads, num_kv_heads)
 
     # selection with current or past query
     if token_budget > 0:
@@ -199,7 +240,7 @@ def quest_arkv_attn(self,
             else:
                 probe_q = past_q
 
-        max_qk = quest_sel(probe_q, min_k_for_sel, max_k_for_sel, self.GQA_policy, num_heads, num_kv_heads)
+        max_qk = _score(probe_q)
         # [bsz, num_kv_heads, page_budget]
         _, sel_page_indices = torch.topk(max_qk, page_budget, dim=-1)
     
@@ -207,7 +248,7 @@ def quest_arkv_attn(self,
     if self.layer_idx > 0 and ll_page_budget > 0:
         llq_ptr = (self.last_layer_attn.q_ptr - 1) % self.spec_ret_steps
         llq = self.last_layer_attn.q_cache[:, llq_ptr, ...]
-        max_qk = quest_sel(llq, min_k_for_sel, max_k_for_sel, self.GQA_policy, num_heads, num_kv_heads)
+        max_qk = _score(llq)
         _, ll_sel_page_indices = torch.topk(max_qk, ll_page_budget, dim=-1)
         # [bsz, num_kv_heads, (up to) page_budget + ll_page_budget]
         # still maybe repeated pages...
diff --git a/accuracy/kvc/patch/llama.py b/accuracy/kvc/patch/llama.py
index 7e50cbb..a95ed5e 100644
--- a/accuracy/kvc/patch/llama.py
+++ b/accuracy/kvc/patch/llama.py
@@ -504,19 +504,51 @@ def enable_llama_dyn_attention(
             module.GQA_policy = config["GQA_policy"]
             module.alloc_len = 512
             module.num_pages = 0    # === reset needed ===
-            max_num_pages = 128 * 1024 // module.page_size
+            # Sized for the ACTUAL truncation length, not a fixed 128K. The
+            # slabs still grow on demand (see dynamic_attention), so this is
+            # only a starting size -- but for Qwen-14B at 64K the 128K sizing
+            # costs 4.1 GB of summaries and pushes a 48 GB card into OOM.
+            _max_tok = int(os.environ.get("KVC_MAX_TOKENS", 128 * 1024))
+            max_num_pages = _max_tok // module.page_size
             if module.token_budget > 0:
                 module.budget_ones = torch.ones((bsz, module.token_budget), 
                                                 device=module.q_proj.weight.device)
-            module.min_k = torch.empty((bsz, max_num_pages, num_dyn_attn_heads, module.head_dim), 
+            # For page_rep=="oval" their (max,min) digest is never written or
+            # read, so allocate it collapsed: dim 1 must stay correct because
+            # the growth check and the *_for_sel slices key off it.
+            _dw = 1 if config.get("page_rep") == "oval" else module.head_dim
+            module.min_k = torch.empty((bsz, max_num_pages, num_dyn_attn_heads, _dw), 
                                        device=module.q_proj.weight.device, dtype=dtype)
-            module.max_k = torch.empty((bsz, max_num_pages, num_dyn_attn_heads, module.head_dim), 
+            module.max_k = torch.empty((bsz, max_num_pages, num_dyn_attn_heads, _dw), 
                                        device=module.q_proj.weight.device, dtype=dtype)
             module.dynamic_attn = types.MethodType(quest_arkv_attn, module)
             if method == "arkv":
                 module.page_rep = "arkv"
             else:   # for quest or spec_kv
                 module.page_rep = config.get("page_rep", "quest")
+            if module.page_rep == "oval":
+                # Our summary is basis + coefficients + centroid, which does not
+                # fit their (max,min) digest slabs, so it gets its own -- indexed
+                # by the same page index. UNQUANTIZED (model dtype).
+                from .oval import publish_metric as _oval_metric
+                module.oval_eta = float(config.get("eta", 0.0))
+                module.oval_rank = int(config.get("oval_rank", 8))
+                _dev = module.q_proj.weight.device
+                _r = module.oval_rank
+                module.oval_basis = torch.empty(
+                    (bsz, max_num_pages, num_dyn_attn_heads, module.head_dim, _r),
+                    device=_dev, dtype=dtype)
+                module.oval_coef = torch.empty(
+                    (bsz, max_num_pages, num_dyn_attn_heads, module.page_size, _r),
+                    device=_dev, dtype=dtype)
+                module.oval_mu = torch.empty(
+                    (bsz, max_num_pages, num_dyn_attn_heads, module.head_dim),
+                    device=_dev, dtype=dtype)
+                module.oval_Gh = None
+                if module.oval_eta > 0.0:
+                    module.oval_Gh = _oval_metric(
+                        module.o_proj.weight, num_dyn_attn_heads, module.head_dim, None
+                    ).to(_dev)
             module.ll_token_budget = 0
             if method == "spec_ret":
                 module.ll_token_budget = config["llb"]
diff --git a/accuracy/kvc/patch/step_update.py b/accuracy/kvc/patch/step_update.py
index e91fc38..9191a23 100644
--- a/accuracy/kvc/patch/step_update.py
+++ b/accuracy/kvc/patch/step_update.py
@@ -48,6 +48,15 @@ class SpecRetUpdater(QuestUpdater):
             layer.self_attn.num_pages = 0
             layer.self_attn.num_correct = 0
             layer.self_attn.num_correct_kv_heads = torch.tensor([0], device=self.model.device)
+            # BUG (theirs): q_ptr / q_cache were never reset between items, so a
+            # new sample began selecting pages with the PREVIOUS sample's
+            # queries. Measured on LongGenBench/llama: the same Block items give
+            # CR 69.03 standalone but 39.17 at position 300 of a 400-item run,
+            # with generation collapsing from 11496 to 1605 tokens right after
+            # the items that saturate the 16K cap.
+            if hasattr(layer.self_attn, "q_ptr"):
+                layer.self_attn.q_ptr = 0
+                layer.self_attn.q_cache.zero_()
 
     def finish(self):
         num_correct_layers = []
diff --git a/accuracy/kvc/patch/tuple_kv_cache.py b/accuracy/kvc/patch/tuple_kv_cache.py
index 178fa0a..d640323 100644
--- a/accuracy/kvc/patch/tuple_kv_cache.py
+++ b/accuracy/kvc/patch/tuple_kv_cache.py
@@ -137,6 +137,17 @@ def old_flash_attention_2_forward(
             self.v_zero = torch.cat([self.v_zero, v_zero], dim=1)
             past_key_value = (torch.cat([past_key_value[0], new_quant_k], dim=1), 
                               torch.cat([past_key_value[1], new_quant_v], dim=1)) if use_cache else None
+    else:
+        # UPSTREAM BUG: the cache was only ever written on the kv8 (int8) path.
+        # In bf16 -- the default, and what --method full runs -- past_key_value
+        # was returned UNCHANGED, so every decode step got a tuple of Nones,
+        # saw an empty cache, and re-RoPEd the new token at position 0. Prefill
+        # is correct, so the first generated token is right and everything after
+        # it is garbage; on InfiniteBench that reads as FullKV scoring 9.39,
+        # passing only the tasks graded on their first token.
+        # key_states/value_states are already the concatenated full sequence at
+        # this point (see the cat above), so the cache is just those.
+        past_key_value = (key_states, value_states) if use_cache else None
 
     attn_output = attn_output.reshape(bsz, q_len, self.hidden_size).contiguous()
     attn_output = self.o_proj(attn_output)
@@ -383,7 +394,10 @@ def old_llama_model_forward(
     seq_length_with_past = seq_length
     past_key_values_length = 0
 
-    if past_key_values is not None:
+    # Upstream checks only that past_key_values is not None, but on the FIRST
+    # forward pass transformers passes a TUPLE OF NONES, so past_key_values[0]
+    # is None and the subscript raises. This is why `--method full` crashes.
+    if past_key_values is not None and past_key_values[0] is not None:
         past_key_values_length = past_key_values[0][0].shape[1]
         seq_length_with_past = seq_length_with_past + past_key_values_length
 
diff --git a/config/model2maxlen.json b/config/model2maxlen.json
index 695c078..659212d 100644
--- a/config/model2maxlen.json
+++ b/config/model2maxlen.json
@@ -1,7 +1,8 @@
 {
-	"qwen-2.5-chat-7b": 120000,
-	"llama-3.1-chat-8b": 120000,
-	"ds-r1-qwen-7b": 120000,
-	"ds-r1-llama-8b": 120000,
-	"ds-r1-qwen-14b": 120000
-}
+ "qwen-2.5-chat-7b": 65536,
+ "llama-3.1-chat-8b": 65536,
+ "ds-r1-qwen-7b": 65536,
+ "ds-r1-llama-8b": 65536,
+ "ds-r1-qwen-14b": 65536,
+ "qwen-2.5-chat-14b": 65536
+}
\ No newline at end of file
diff --git a/config/model2path.json b/config/model2path.json
index df82a90..28b5d7f 100644
--- a/config/model2path.json
+++ b/config/model2path.json
@@ -1,7 +1,8 @@
 {
-	"qwen-2.5-chat-7b": "Qwen/Qwen2.5-7B-Instruct",
-	"llama-3.1-chat-8b": "meta-llama/Llama-3.1-8B-Instruct",
-	"ds-r1-qwen-7b": "deepseek-ai/DeepSeek-R1-Distill-Qwen-7B",
-	"ds-r1-llama-8b": "deepseek-ai/DeepSeek-R1-Distill-Llama-8B",
-	"ds-r1-qwen-14b": "deepseek-ai/DeepSeek-R1-Distill-Qwen-14B"
+ "qwen-2.5-chat-7b": "Qwen/Qwen2.5-7B-Instruct",
+ "llama-3.1-chat-8b": "NousResearch/Meta-Llama-3.1-8B-Instruct",
+ "ds-r1-qwen-7b": "deepseek-ai/DeepSeek-R1-Distill-Qwen-7B",
+ "ds-r1-llama-8b": "deepseek-ai/DeepSeek-R1-Distill-Llama-8B",
+ "ds-r1-qwen-14b": "deepseek-ai/DeepSeek-R1-Distill-Qwen-14B",
+ "qwen-2.5-chat-14b": "Qwen/Qwen2.5-14B-Instruct"
 }
\ No newline at end of file
diff --git a/source/freekv/adapter/modeling.py b/source/freekv/adapter/modeling.py
index f9abf0a..9d7ce65 100644
--- a/source/freekv/adapter/modeling.py
+++ b/source/freekv/adapter/modeling.py
@@ -146,7 +146,7 @@ def _freekv_attn_forward(
                     evt = torch.cuda.Event()
                     evt.record(state.prefill_backup_stream)
                     state.prefill_backup_events[cur_id] = evt
-                state.prefill_save_digests(cur_id, key_states)
+                state.prefill_save_digests(cur_id, key_states, value_states)
             attn_output = state.prefill_sdpa(cur_id, query_states)
             infer_state.prefill_evict_extra_pages(
                 cur_id, query_states[:, -1:, ...].contiguous()
@@ -227,6 +227,12 @@ def _freekv_attn_forward(
 
                     if state.corr is not None:
                         state.last_step_q[cur_id] = query_states
+                elif state.method == "raas":
+                    # dropping method: score pages with the engine's own min/max
+                    # bound, refresh timestamps, arm the oldest slot for
+                    # permanent eviction. No top-k, no recall, no transfer.
+                    state.raas_step(cur_id, query_states)
+                    attn_output = state.decode_sdpa(cur_id, query_states, attn_page_ids)
                 else:
                     state.estimate_select_recall(cur_id, query_states)
                     attn_output = state.decode_sdpa(cur_id, query_states, attn_page_ids)
@@ -277,6 +283,13 @@ def enable_offload(
         self.lm_head.forward = lambda x: \
             _lm_head_forward_streamed(x, _lm_head_forward, infer_state=infer_state)
 
+    # stash o_proj per layer for the OVAL output-aware metric (eta > 0)
+    if getattr(infer_state, "page_rep", "quest") == "oval" and infer_state.oval_eta > 0:
+        for mod in self.modules():
+            if "Attention" in str(mod.__class__) and hasattr(mod, "layer_idx"):
+                infer_state.oval_publish_metric(mod.layer_idx, mod.o_proj.weight)
+        print(f"[oval] stashed o_proj for {len(infer_state.oval_oproj)} layers")
+
     for mod in self.modules():
         mod_cls = str(mod.__class__)
         if "Attention" in mod_cls:
diff --git a/source/freekv/infer_state.py b/source/freekv/infer_state.py
index efa74df..d4f6233 100644
--- a/source/freekv/infer_state.py
+++ b/source/freekv/infer_state.py
@@ -7,6 +7,8 @@ from torch import Tensor
 
 from .kv_cache import KvPool, KvCache
 from . import kernels
+from . import oval_summary
+from . import raas as raas_mod
 from . import utils
 import atexit
 
@@ -53,6 +55,20 @@ class InferState:
         self.dtype = dtype
         self.device = device
 
+        # ---- OVAL: our output-aware page basis (eta=0 recovers LOCKS) -----
+        self.page_rep = kwargs.get("page_rep", "quest")
+        self.oval_rank = int(kwargs.get("oval_rank", 8))
+        self.oval_eta = float(kwargs.get("eta", 0.0))
+        self.oval_max_pages = int(kwargs.get("oval_max_pages", 4096))
+        self.oval = None
+        # o_proj stashed at patch time; G^{1/2} built lazily (enable_offload
+        # runs BEFORE the oval store is allocated)
+        self.oval_oproj = {}
+        # ---- RaaS dropping method (Fig 6 baseline, not in the artifact) ----
+        self.method = kwargs.get("method", "freekv")
+        self.raas_alpha = float(kwargs.get("raas_alpha", 0.02))
+        self.raas = None
+
         if n_max_pages is None:
             assert n_max_bytes is not None
             n_max_pages = n_max_bytes // (
@@ -141,7 +157,8 @@ class InferState:
         self.dg_indptrs: Tensor = None
 
         # maybe larger for larger batch
-        wbufs = [torch.empty(64 * 1024 * 1024, **self._u8) for _ in self.budget2layers]
+        _wmib = int(os.environ.get("FREEKV_WORKSPACE_MIB", "64"))
+        wbufs = [torch.empty(_wmib * 1024 * 1024, **self._u8) for _ in self.budget2layers]
         self.prefill_handler = kernels.BatchPrefillWithPagedKVCacheWrapper(
             wbufs[0], self.layout
         )
@@ -279,6 +296,34 @@ class InferState:
             self.layer2budget[i] and KvCache(self._pool, bsz)
             for i in range(self.n_layers)
         ]
+        if self.method == "raas" and self.raas is None:
+            _b = max([x for x in self.layer2budget if x] + [0])
+            self.raas = raas_mod.RaasState(self.n_layers, bsz, _b,
+                                           self.n_groups, self.device)
+            # RaaS drops pages, so it only ever has `_b` of them to score --
+            # scoring FreeKV's full digest instead costs +4.39 ms/step (13% of
+            # the bar). See RaasDigest for the measurement.
+            self.raas_dg = raas_mod.RaasDigest(
+                self.n_layers, bsz, _b, self.page_size, self.n_kv_heads,
+                self.head_dim, self.dtype, self.device)
+            print(f"[raas] dropping method active: budget={_b} pages, "
+                  f"alpha={self.raas_alpha}, no offload / no recall")
+        elif self.method == "raas":
+            self.raas.reset()
+            self.raas_dg.n_tokens = [0] * self.n_layers
+        if self.page_rep == "oval" and (
+                self.oval is None or self.oval.bsz != bsz):
+            self.oval = oval_summary.OabSummaryStore(
+                self.n_layers, bsz, self.oval_max_pages, self.n_kv_heads,
+                self.head_dim, self.oval_rank, self.oval_eta,
+                self.dtype, self.device)
+            print(f"[oval] page_rep=oval rank={self.oval_rank} "
+                  f"eta={self.oval_eta} "
+                  f"G={'W^T W' if self.oval_eta > 0 else 'off (eta=0)'} "
+                  f"buffers={self.oval.bytes()/1e6:.0f}MB")
+        elif self.page_rep == "oval":
+            self.oval.n_pages = [0] * self.n_layers
+
         self._cpu_pool.clear()
         self.cpu_kv_caches = [
             self.layer2budget[i] and KvCache(self._cpu_pool, bsz)
@@ -297,7 +342,12 @@ class InferState:
 
         self.topk_buff = torch.empty([self.n_layers, bsz1 * (1 << 10)], **self._u8)
         # we do not pre-allocate real kv-pages before prefill
-        [kvc.prefill_alloc_n_tokens(q_len) for kvc in self.cpu_kv_caches if kvc]
+        # A dropping method has no host tier: it never offloads and never
+        # recalls, so allocating (and filling) the prompt's KV on the CPU is a
+        # porting artifact of living inside FreeKV's engine. Skipping it is
+        # what makes the host-memory column mean the same thing for every arm.
+        if self.method != "raas":
+            [kvc.prefill_alloc_n_tokens(q_len) for kvc in self.cpu_kv_caches if kvc]
         n_kv_pages = (q_len + self.page_size - 1) // self.page_size
         self.kv_last_page_len = (q_len - 1) % self.page_size + 1
         self.kv_last_page_lens = torch.tensor(
@@ -369,7 +419,8 @@ class InferState:
         self.kv_last_page_lens = torch.tensor(
             [self.kv_last_page_len] * bsz, **self._i32
         )
-        [kvc.decode_alloc_1_token() for kvc in self.cpu_kv_caches if kvc]
+        if self.method != "raas":
+            [kvc.decode_alloc_1_token() for kvc in self.cpu_kv_caches if kvc]
 
         if n_new_kv_pages > 0:
             assert n_new_kv_pages == 1
@@ -471,6 +522,23 @@ class InferState:
     ):
         if n_groups is None:
             n_groups = self.n_groups
+        if self.page_rep == "oval":
+            return self._oval_scores(layer_idx, query_states, n_groups)
+        # RaaS scores only its resident pages -- but ONLY during decode. At
+        # prefill every method, RaaS included, must choose which pages of the
+        # prompt to keep, and that selection is necessarily over all of them;
+        # the compact digest is not populated until after that eviction, so
+        # n_tokens == 0 is exactly the "still prefilling" signal. Fig. 6
+        # measures decode TBT (tbt_stat_ms[1:]), so prefill cost is excluded
+        # from the bar either way.
+        if (self.method == "raas" and getattr(self, "raas_dg", None) is not None
+                and self.raas_dg.n_tokens[layer_idx] > 0):
+            rd = self.raas_dg
+            return kernels.estimate_scores(
+                query_states, rd.buffer[layer_idx], rd.indices, rd.indptr,
+                rd.last_page_lens(layer_idx), rd.n_tokens[layer_idx],
+                self.layout, n_groups,
+            )
         dgc = self.dg_caches[layer_idx]
         return kernels.estimate_scores(
             query_states,
@@ -622,10 +690,13 @@ class InferState:
             ng, gs, ns, nw, 
             self.recall_buf1, self.recall_buf2,
             recall_stream1.cuda_stream, recall_stream2.cuda_stream,
-            recall_evt1.cuda_event, recall_evt2.cuda_event
+            recall_evt1.cuda_event, recall_evt2.cuda_event,
+            *self._oval_spec_bufs(layer_idx),
         )
 
     def prefill_backup_pages(self, layer_idx: int):
+        if self.method == "raas":
+            return          # nothing is offloaded, so nothing to back up
         kvc = self.kv_caches[layer_idx]
         cpu_kvc = self.cpu_kv_caches[layer_idx]
         if kvc.budget is None:
@@ -639,6 +710,8 @@ class InferState:
                 cpu_kvc.buffer[ci].copy_(kvc.buffer[gi], non_blocking=True)
 
     def decode_backup_1_page(self, layer_idx: int):
+        if self.method == "raas":
+            return          # dropping method: evicted pages are gone, not offloaded
         kvc = self.kv_caches[layer_idx]
         cpu_kvc = self.cpu_kv_caches[layer_idx]
         if kvc.budget is None:
@@ -650,6 +723,68 @@ class InferState:
             else:
                 cpu_kvc[i, -2].copy_(kvc[i, kvc.evict_idx], non_blocking=True)
 
+    # ---- oval / raas ------------------------------------------------
+    def oval_publish_metric(self, layer_idx: int, o_proj_weight):
+        if self.oval_eta > 0.0:
+            self.oval_oproj[layer_idx] = o_proj_weight
+
+    def _oval_gh(self, layer_idx: int):
+        if self.oval_eta <= 0.0:
+            return None
+        gh = self.oval.Gh[layer_idx]
+        if gh is None and layer_idx in self.oval_oproj:
+            gh = oval_summary.publish_metric(
+                self.oval_oproj[layer_idx], self.n_kv_heads, self.head_dim,
+                self.n_qo_heads // self.n_kv_heads)
+            self.oval.Gh[layer_idx] = gh
+        return gh
+
+    def _oval_build(self, layer_idx: int, paged_k: Tensor, paged_v: Tensor,
+                     first_page: int):
+        st = self.oval
+        st._ensure_coef(self.page_size)
+        mu, bs, cf = oval_summary.build_summary(
+            paged_k, paged_v, self._oval_gh(layer_idx), st.eta, st.rank)
+        n = mu.shape[1]
+        sl = slice(first_page, first_page + n)
+        st.mu[layer_idx, :, sl] = mu
+        st.basis[layer_idx, :, sl] = bs
+        st.coef[layer_idx, :, sl] = cf
+        st.n_pages[layer_idx] = first_page + n
+
+    def _oval_spec_bufs(self, layer_idx: int):
+        """(basis, coef, mu) slices for the fused spec_ret call, or (None,)*3."""
+        if self.page_rep != "oval":
+            return (None, None, None)
+        st = self.oval
+        n = st.n_pages[layer_idx]
+        return (st.basis[layer_idx, :, :n].contiguous(),
+                st.coef[layer_idx, :, :n].contiguous(),
+                st.mu[layer_idx, :, :n].contiguous())
+
+    def _oval_scores(self, layer_idx: int, q: Tensor, n_groups: int):
+        st = self.oval
+        n = st.n_pages[layer_idx]
+        b = self.batch_size
+        qv = q.reshape(b, self.n_qo_heads, self.head_dim)
+        if os.environ.get("OVAL_TORCH_SCORER") == "1":
+            return oval_summary.score_pages(
+                qv, st.mu[layer_idx, :, :n], st.basis[layer_idx, :, :n],
+                st.coef[layer_idx, :, :n], self.n_qo_heads, self.n_kv_heads,
+                n_groups)
+        return kernels.oval_estimate_scores(
+            qv.contiguous(), st.basis[layer_idx, :, :n].contiguous(),
+            st.coef[layer_idx, :, :n].contiguous(),
+            st.mu[layer_idx, :, :n].contiguous(), n_groups)
+
+    def raas_step(self, layer_idx: int, q: Tensor):
+        """Score pages, refresh timestamps, arm the victim slot for eviction."""
+        kvc = self.kv_caches[layer_idx]
+        scores = self.estimate_scores(layer_idx, q)
+        kvc.raas_evict_idx = raas_mod.refresh_and_pick_victim(
+            self.raas, layer_idx, scores, kvc, kvc.seq_len, self.raas_alpha,
+            self.n_sink_pages, self.n_win_pages)
+
     def _summarize_keys(self, filled_keys: Tensor) -> Digest:
         maxs = filled_keys.max(dim=2).values
         mins = filled_keys.min(dim=2).values
@@ -666,14 +801,21 @@ class InferState:
         maxs = centers + dists
         return maxs, mins
 
-    def prefill_save_digests(self, layer_idx: int, keys: Tensor):
+    def prefill_save_digests(self, layer_idx: int, keys: Tensor,
+                             values: Tensor = None):
         if self.layer2budget[layer_idx] is None:
             return
         bsz, q_len, *_ = keys.shape
         n_filled_pages = (q_len + self.page_size - 1) // self.page_size - 1
-        filled_keys = keys[:, : n_filled_pages * self.page_size, ...].reshape(
-            bsz, n_filled_pages, self.page_size, self.n_kv_heads, self.head_dim
-        )
+        shp = (bsz, n_filled_pages, self.page_size, self.n_kv_heads, self.head_dim)
+        filled_keys = keys[:, : n_filled_pages * self.page_size, ...].reshape(*shp)
+        if self.page_rep == "oval":
+            # the output-aware term needs VALUES, which their digest path never
+            # plumbs through (it summarises keys only)
+            fv = values[:, : n_filled_pages * self.page_size, ...].reshape(*shp) \
+                if values is not None else filled_keys
+            self._oval_build(layer_idx, filled_keys, fv, 0)
+            return
         self.save_digests(layer_idx, self._summarize_keys(filled_keys))
 
     def decode_save_1_digest(self, layer_idx: int):
@@ -684,6 +826,26 @@ class InferState:
             [kvc[i, kvc.evict_idx][:1][None, ...] for i in range(self.batch_size)],
             dim=0,
         )
+        if self.method == "raas":
+            # the page just placed lives in slot evict_idx (negative during the
+            # growth phase, before the budget is full)
+            slot = kvc.evict_idx
+            if slot < 0:
+                slot = kvc.n_real_pages + slot
+            # filled_keys is already [bsz, 1, page, n_kv, d]
+            mx, mn = self._summarize_keys(filled_keys)
+            self.raas_dg.write_slot(layer_idx, int(slot),
+                                    mx[:, 0], mn[:, 0])
+            return
+        if self.page_rep == "oval":
+            # kvc[i, idx] is [2, page, n_kv, d]: index 0 is K, 1 is V
+            filled_vals = torch.cat(
+                [kvc[i, kvc.evict_idx][1:2][None, ...]
+                 for i in range(self.batch_size)], dim=0,
+            )
+            self._oval_build(layer_idx, filled_keys, filled_vals,
+                              self.oval.n_pages[layer_idx])
+            return
         self.save_digests(layer_idx, self._summarize_keys(filled_keys))
 
     def prefill_evict_extra_pages(self, layer_idx: int, query_states: Tensor):
@@ -719,6 +881,12 @@ class InferState:
             .expand(bsz, ng, kvc.gc2cc.shape[-1])
             .contiguous()
         )
+        if self.method == "raas" and getattr(self, "raas_dg", None) is not None:
+            # slot j of the compact digest == GPU slot j. GPU pool is always
+            # NHD, so page_shape is (2, page, n_kv, d) and index 0 is K.
+            keys = self._pool.buffer[kvc.c2p][:, :, 0]
+            mx, mn = self._summarize_keys(keys)
+            self.raas_dg.write_all(layer_idx, mx, mn)
 
     def alloc_page(self):
         if len(self._pool._free_ids) <= 0:
diff --git a/source/freekv/kernels.py b/source/freekv/kernels.py
index ad11d7a..3c158da 100644
--- a/source/freekv/kernels.py
+++ b/source/freekv/kernels.py
@@ -84,6 +84,16 @@ def append_paged_kv_cache(
     )
 
 
+def oval_estimate_scores(
+    q: torch.Tensor,       # [bsz, n_qo_heads, head_dim]
+    basis: torch.Tensor,   # [bsz, n_pages, n_kv, head_dim, rank]
+    coef: torch.Tensor,    # [bsz, n_pages, n_kv, page_size, rank]
+    mu: torch.Tensor,      # [bsz, n_pages, n_kv, head_dim]
+    n_groups: int = 1,
+) -> torch.Tensor:         # [bsz, n_groups, n_pages]
+    return _cpp.oval_estimate_scores(q, basis, coef, mu, n_groups)
+
+
 def estimate_scores(
     q: torch.Tensor,  # [bsz, 1, num_heads, head_dim]
     dg_data: torch.Tensor,  # [n_max_pages, 2, page_size, n_kv_heads, head_dim]
@@ -435,7 +445,10 @@ def estimate_select_recall_pool(
     recall_buf1, recall_buf2,
     stream1, stream2,
     event1, event2,
+    oval_basis=None, oval_coef=None, oval_mu=None,
 ):
+    # empty tensors => the C++ side uses their min/max digest path
+    _e = torch.empty(0, device=query_states.device, dtype=query_states.dtype)
     _cpp.estimate_select_recall_pool(
         # for estimate
         query_states,
@@ -452,6 +465,9 @@ def estimate_select_recall_pool(
         recall_buf1, recall_buf2,
         stream1, stream2,
         event1, event2,
+        _e if oval_basis is None else oval_basis,
+        _e if oval_coef is None else oval_coef,
+        _e if oval_mu is None else oval_mu,
     )
 
 def alloc_managed_bool(rows: int, cols: int):
diff --git a/source/freekv/kv_cache.py b/source/freekv/kv_cache.py
index b77907c..8fa91d7 100644
--- a/source/freekv/kv_cache.py
+++ b/source/freekv/kv_cache.py
@@ -172,7 +172,13 @@ class KvCache:
                 # [bsz, n_groups] <= [bsz,]
                 self.cc2gp[..., -1] = self.c2p[..., -1].unsqueeze(-1)
         else:
-            if self.n_win_pages > 1:
+            # RaaS (a DROPPING method) supplies the victim slot itself: the
+            # page with the oldest timestamp. FreeKV instead rotates a window
+            # slot, because for it no page is ever really dropped.
+            _raas_victim = getattr(self, "raas_evict_idx", None)
+            if _raas_victim is not None:
+                e_gci = int(_raas_victim)
+            elif self.n_win_pages > 1:
                 e_gci = (
                     self.budget
                     - self.n_win_pages
diff --git a/source/freekv_cpp/src/api.cu b/source/freekv_cpp/src/api.cu
index 318820b..a6db131 100644
--- a/source/freekv_cpp/src/api.cu
+++ b/source/freekv_cpp/src/api.cu
@@ -10,6 +10,7 @@ PYBIND11_MODULE(freekv_cpp, m) {
 	m.def("append_paged_kv_cache_prefill", &append_paged_kv_cache_prefill);
 	m.def("append_paged_kv_cache_decode", &append_paged_kv_cache_decode);
 	m.def("estimate_scores", &estimate_scores);
+	m.def("oval_estimate_scores", &oval_estimate_scores);
 	m.def("select_topk", &select_topk);
 	m.def("prefill_select_topk", &prefill_select_topk);
   py::class_<BatchPrefillWithPagedKVCachePyTorchWrapper>(
diff --git a/source/freekv_cpp/src/estimate_select_recall.cu b/source/freekv_cpp/src/estimate_select_recall.cu
index 8392b5e..2f53bea 100644
--- a/source/freekv_cpp/src/estimate_select_recall.cu
+++ b/source/freekv_cpp/src/estimate_select_recall.cu
@@ -41,10 +41,15 @@ void estimate_select_recall_core(
     uint64_t stream_handle1,
     uint64_t stream_handle2,
     uint64_t event_handle1,
-    uint64_t event_handle2
+    uint64_t event_handle2,
+    // OVAL page representation; empty tensors => use their min/max digest
+    const torch::Tensor &oval_basis,
+    const torch::Tensor &oval_coef,
+    const torch::Tensor &oval_mu
 ) {
+  const bool use_oval = oval_basis.numel() > 0;
   CHECK_INPUT(q);
-  CHECK_INPUT(dg_data);
+  if (!use_oval) { CHECK_INPUT(dg_data); }
   CHECK_INPUT(dg_indices);
   CHECK_INPUT(dg_indptr);
 
@@ -106,6 +111,7 @@ void estimate_select_recall_core(
   torch::Tensor o =
     torch::empty({batch_size, num_qo_heads, dg_seq_len}, q.options());
 
+  if (!use_oval)
   DISPATCH_PYTORCH_DTYPE_TO_CTYPE(q.scalar_type(), c_type, [&] {
     return DISPATCH_kv_layout(QKVLayout::kNHD, KV_LAYOUT, [&] {
       paged_kv_t<PageStorage::kIndices, KV_LAYOUT, c_type, int32_t> paged_kv(
@@ -126,8 +132,11 @@ void estimate_select_recall_core(
     });
   });
 
-  torch::Tensor scores = 
-    o.reshape({batch_size, n_groups, num_qo_heads / n_groups, dg_seq_len}).mean(2);
+  torch::Tensor scores =
+    use_oval
+      ? oval_estimate_scores(q.reshape({batch_size, num_qo_heads, head_dim}),
+                              oval_basis, oval_coef, oval_mu, n_groups)
+      : o.reshape({batch_size, n_groups, num_qo_heads / n_groups, dg_seq_len}).mean(2);
   scores = scores.reshape({sel_bsz, -1});
   CHECK_INPUT(scores);
 
@@ -191,7 +200,10 @@ void estimate_select_recall_pool(
     uint64_t stream_handle1,
     uint64_t stream_handle2,
     uint64_t event_handle1,
-    uint64_t event_handle2
+    uint64_t event_handle2,
+    const torch::Tensor &oval_basis,
+    const torch::Tensor &oval_coef,
+    const torch::Tensor &oval_mu
 ) {
   TORCH_CHECK(recall_thread_pool);
   recall_thread_pool->enqueue(
@@ -202,6 +214,7 @@ void estimate_select_recall_pool(
     n_groups, gs, n_sink_pages, n_win_pages,
     recall_buf1, recall_buf2, 
     stream_handle1, stream_handle2,
-    event_handle1, event_handle2
-  );
+    event_handle1, event_handle2,
+    oval_basis, oval_coef, oval_mu
+  ).wait();
 }
diff --git a/source/freekv_cpp/src/flashinfer_ops.h b/source/freekv_cpp/src/flashinfer_ops.h
index 4a32ad1..52f1ff7 100644
--- a/source/freekv_cpp/src/flashinfer_ops.h
+++ b/source/freekv_cpp/src/flashinfer_ops.h
@@ -58,6 +58,10 @@ void append_paged_kv_cache_decode(
 	torch::Tensor kv_last_page_len, // [bsz]
 	unsigned int layout);
 
+torch::Tensor oval_estimate_scores(torch::Tensor q, torch::Tensor basis,
+                                    torch::Tensor coef, torch::Tensor mu,
+                                    int64_t n_groups);
+
 torch::Tensor estimate_scores(
 	torch::Tensor q, // [bsz, 1, num_heads, head_dim]
 	torch::Tensor dg_data, // [n_max_pages, 2, page_size, n_kv_heads, head_dim]
@@ -270,8 +274,10 @@ void estimate_select_recall_pool(
     uint64_t stream_handle1,
     uint64_t stream_handle2,
     uint64_t event_handle1,
-    uint64_t event_handle2
-);
+    uint64_t event_handle2,
+    const torch::Tensor &oval_basis,
+    const torch::Tensor &oval_coef,
+    const torch::Tensor &oval_mu);
 
 torch::Tensor alloc_managed_bool(int64_t rows, int64_t cols);
 
diff --git a/source/freekv_cpp/src/thread_pool.h b/source/freekv_cpp/src/thread_pool.h
index f819297..126e3ad 100644
--- a/source/freekv_cpp/src/thread_pool.h
+++ b/source/freekv_cpp/src/thread_pool.h
@@ -40,8 +40,11 @@ public:
     }
 
   template<class F, class... Args>
-  // Use std::function to avoid template in enqueue signature for easier binding
-  void enqueue(F&& f, Args&&... args) {
+  // Returns a future so the caller can order the CPU-side handoff. CUDA events
+  // alone are NOT sufficient: they are pre-recorded at init to be "valid", so a
+  // wait_event issued before the pool thread reaches its cudaEventRecord
+  // consumes the STALE recording and returns immediately.
+  std::future<void> enqueue(F&& f, Args&&... args) {
     auto task = std::make_shared< std::packaged_task<void()> >(
       std::bind(std::forward<F>(f), std::forward<Args>(args)...)
     );
@@ -51,7 +54,7 @@ public:
       tasks.emplace([task](){ (*task)(); });
     }
     condition.notify_one();
-    // Note: We are not returning the future here as Python side will use CUDA events
+    return task->get_future();
   }
 
   ~ThreadPool() {
diff --git a/source/pred.py b/source/pred.py
index a316410..1c4103f 100644
--- a/source/pred.py
+++ b/source/pred.py
@@ -107,6 +107,15 @@ def parse_args(args=None):
     parser.add_argument("--n_recall_stream", type=int, default=2, help="Number of CUDA streams for recall")
     parser.add_argument("--recall_impl", type=str, default="cuda_cpy",
                         choices=["arkvale", "torch_cpy", "cuda_cpy"], help="Recall implementation")
+    parser.add_argument("--method", type=str, default="freekv",
+                        choices=["freekv", "raas"],
+                        help="freekv = their retrieval engine; raas = dropping method")
+    parser.add_argument("--raas_alpha", type=float, default=0.02)
+    parser.add_argument("--page_rep", type=str, default="quest",
+                        choices=["quest", "oval"],
+                        help="their min/max digest, or our rank-r output-aware basis")
+    parser.add_argument("--eta", type=float, default=0.0)
+    parser.add_argument("--oval_rank", type=int, default=8)
     parser.add_argument("--corr", type=float, default=None, help="Correction threshold (cosine similarity); None to disable")
     parser.add_argument("--warmup", type=int, default=2, help="Number of warmup generation rounds before timing")
 
@@ -156,6 +165,11 @@ def load_model_and_tokenizer(path):
             n_recall_stream=args.n_recall_stream,
             recall_impl=args.recall_impl,
             corr=args.corr,
+            method=args.method,
+            raas_alpha=args.raas_alpha,
+            page_rep=args.page_rep,
+            eta=args.eta,
+            oval_rank=args.oval_rank,
         )
     else:
         assert not args.spec_ret
